import { ProcessorConfig } from '../processorConfig'

const config: ProcessorConfig = {
  chainName: 'paseo',
  prefix: 'paseo',
  dataSource: {
    archive: '',
    chain: ''
  }
}

export default config
